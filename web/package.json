{
  "name": "ai-sentiment-sonification-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview --host --port ${PORT:-4173}",
    "start": "serve -s dist -l ${PORT:-4173}",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "d3": "^7.9.0",
    "serve": "^14.2.3",
    "tone": "^15.0.4"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "typescript": "^5.5.4",
    "vite": "^5.4.8"
  }
}
